/**
 * Enum of the actions those can be done with a shape by the player.
 * @type {{DOWN: number, LEFT: number, ROTATE: number, RIGHT: number, SET: 4}}
 */
export const Actions = {LEFT: 0, RIGHT: 1, DOWN: 2, ROTATE: 3, SET: 4};
Object.freeze(Actions);